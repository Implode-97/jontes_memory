Use `/Users/jnyjc2/.codex/skills/recall-memory` at the start of tasks where prior Obsidian memory may contain relevant user preferences, project decisions, or domain conventions.
Use `/Users/jnyjc2/.codex/skills/capture-memory` after substantive conversations when a reusable learning should be stored in short-term Obsidian memory.

This repository also keeps portable copies of the memory skills under `skills/` so they can be reused or installed elsewhere.
