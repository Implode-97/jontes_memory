---
type: short-term-learning
topic: git
created: 2026-07-13 10:49:41
source: codex-conversation
confidence: high
---

# Learning: MR 32 rollback exactly removed faulty master history

## Context
GitLab MR !32 in `data-platform-terragrunt-catalog` was rebased onto accidental master commits `be78e75` and `184cf27`, then rewritten after master was restored to `7fc05e3`.

## Learning
The current MR head `6afce9c` is seven feature commits directly above `7fc05e3`; neither faulty commit is an ancestor. GitLab diff version `2215369` records the contaminated rebase onto `184cf27`, while version `2215643` records the rollback. The current tree ID `e710388a` exactly matches the first clean rebase head `3e5bcfb`, and `git range-diff` reports all seven patches identical.

## Future Use
Treat MR !32's branch rollback as verified. If its pipeline fails, investigate the current code and target-base integration rather than attributing the failure to lingering remote-state commits.
