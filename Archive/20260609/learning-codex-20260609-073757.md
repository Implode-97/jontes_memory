---
type: short-term-learning
topic: codex
created: 2026-06-09 07:37:57
source: codex-conversation
confidence: high
---

# Learning: global multi-agent scoring skill

## Context
The user asked to create a global/user-scoped skill that preserves the multi-subagent code scoring workflow used in this session.

## Learning
The user wants `$score-code-with-agents` available globally from `/Users/jnyjc2/.codex/skills/score-code-with-agents`. The expected behavior is to spawn several independent, codebase-relevant subagents, pass local style, memory, adjacent code, and current primary-doc context when relevant, and synthesize individual 0-100 scores for readability, KISS, YAGNI, maintainability, structural simplicity, and safety-aware simplification. The workflow should stay read-only unless edits are explicitly requested, should not run local tests for score-only reviews unless asked, and should propagate explicit reasoning-effort requests such as `xhigh` to each subagent.

## Future Use
When the user asks to "SPAWN", "do the same scoring", "score this feature", or similar, use `$score-code-with-agents` and preserve this multi-agent review pattern instead of doing a single-agent opinion pass.
