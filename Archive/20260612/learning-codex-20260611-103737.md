---
type: short-term-learning
topic: codex
created: 2026-06-11 10:37:37
source: codex-conversation
confidence: high
---

# Learning: Jira MCP formatting skill has user-wide metadata

## Context
The repo-local `/Users/jnyjc2/code/vibe_code/sandbox_catalog/.agents/skills/jira-mcp-formatting` directory contained only `agents/openai.yaml`, while the canonical user-wide skill already had the full `SKILL.md`.

## Learning
The user-wide installed copy at `/Users/jnyjc2/.codex/skills/jira-mcp-formatting` now includes both `SKILL.md` and `agents/openai.yaml`. The metadata declares the Jira MCP dependency and allows implicit invocation, while the skill body remains the source of the Jira-safe formatting workflow.

## Future Use
When a future session needs to use or maintain the Jira MCP formatting skill, treat `/Users/jnyjc2/.codex/skills/jira-mcp-formatting` as the canonical user-wide location. If autocomplete or implicit invocation does not pick up the new metadata immediately, restart Codex or open a new thread before editing the skill contents.
