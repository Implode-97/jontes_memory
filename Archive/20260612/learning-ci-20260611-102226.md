---
type: short-term-learning
topic: ci
created: 2026-06-11 10:22:26
source: codex-conversation
confidence: high
---

# Learning: Use ci-log-triage for GitLab trace failures

## Context
During ng-iac pipeline investigation, the user pointed to a dedicated local skill at `/Users/jnyjc2/code/vibe_code/sandbox_catalog/skills/ci-log-triage/SKILL.md`.

## Learning
For failed GitLab CI jobs, especially Terratest/Terragrunt E2E logs, prefer fetching the raw GitLab job trace and running the skill's Python filter script before diagnosing. The script lives at `/Users/jnyjc2/code/vibe_code/sandbox_catalog/skills/ci-log-triage/scripts/filter_ci_log.py` and reduces noisy CI output to the earliest likely failure block. The user also prefers not running local tests when the CI pipeline already shows pass/fail status.

## Future Use
When asked to review ng-iac pipeline failures, identify the exact failed job, download its raw trace with `glab api`, run `filter_ci_log.py`, then diagnose from the filtered output first. Treat later cleanup or diagnostic errors as secondary unless the filtered first failure points there.
