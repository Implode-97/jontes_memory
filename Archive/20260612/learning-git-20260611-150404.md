---
type: short-term-learning
topic: git
created: 2026-06-11 15:04:04
source: codex-conversation
confidence: high
---

# Learning: CAV-131446 compute policy branch forward-ported

## Context
Santosh's catalog branch `origin/feature/syahb2/CAV-131446-databricks-cluster-policy` was based on the old pre-squash E2E branch. The user wanted the same integration approach used for Najvoa's branch: create a fresh Codex branch from the corrected Najvoa/CAV-133921 branch and selectively port Santosh's feature work.

## Learning
The new branch is `feature/codex/CAV-131446-compute-policy-on-CAV-133921-platform-e2e`, pushed to origin at commit `9c10e38`. It is based on `feature/codex/CAV-133921-serverless-budget-policy-e2e-on-platform-e2e` at `dcac21e`. The port keeps Santosh's `dbx_compute_policy` unit, guard tests, examples, and docs, but rewrites the E2E coverage into modular stage files using `readUnitOutputMap` and `requireOutputValue` instead of the older single-file/state-helper approach.

## Future Use
For continued CAV-131446 catalog review, compare the new branch against the corrected CAV-133921 branch or its eventual target, not Santosh's original branch. The intentional differences from Santosh's original delta are no E2E `go.mod`/`go.sum` changes for old Terraform-state helpers, plus new modular E2E stage files and updated architecture docs.
