---
type: short-term-learning
topic: ci
created: 2026-07-06 11:36:34
source: codex-conversation
confidence: high
---

# Learning: Catalog CI Role Now Allows S3 Resource Tagging

## Context
After diagnosing MR !31 `platform-e2e-test` S3 backend bootstrap failures, the deployed IAM role `traton-dmp-oidc-catalog-deploy-role` in account `498873881431` was updated directly.

## Learning
The inline policy `traton-dmp-oidc-catalog-deploy-policy` now includes `s3:TagResource` and `s3:UntagResource` in the `S3BucketLifecycleAndConfigForTerraformTests` statement, alongside existing bucket lifecycle/configuration permissions. IAM simulation for `arn:aws:s3:::traton-dmp-498873881431-data-platform-tg-catalog-e2e-tfstate` returns `allowed` for `s3:CreateBucket`, `s3:TagResource`, `s3:UntagResource`, and `s3:PutBucketTagging`, with Organizations allowing the actions.

## Future Use
For the next MR !31 E2E run, do not treat the previous `s3:TagResource` gap as still open unless a fresh CloudTrail event proves otherwise. If bootstrap still fails, inspect the new first CloudTrail error rather than re-fixing this permission.
